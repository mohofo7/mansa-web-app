import React from "react";
import styles from '../../styles/utils.module.scss';
const NavBar: React.FC = () => {
    return (
        <div className={styles.headingXL}>Mansa</div>
    );
};

export default NavBar;